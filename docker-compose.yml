version: "3.9"

services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8001:8000"
    env_file:
      - .env
    environment:
      PORT: "8000"
    volumes:
      - ./storage:/app/storage
      - ./data:/app/data
